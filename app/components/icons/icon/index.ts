/**
 * File to Keep track of all the used custom Icon fonts within the App.
 *
 * Not these will be later replaced with custom SVG icons that can be
 * fine controlled for things like micro animations.
 */

export type { TIconProps } from './icon.types';

export * from './font-icons';
export { Icon } from './IconElements';
